#include "classes.h"
void process_raw_inventory(std::vector<std::string> raw_inventory, Player &User);
void process_raw_status(Player &User, level &Current, Csr &csr_pos);
void process_raw_user(Player &User);
